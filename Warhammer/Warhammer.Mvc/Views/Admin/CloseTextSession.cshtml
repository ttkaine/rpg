@model Warhammer.Core.Entities.Session

@{
    ViewBag.Title = "Close Text Sessione";
}
<div class="content-wrapper">

    <div class="main-content">
        <div class="content-block">
            <h2>Closing @Html.ActionLink(Model.FullName, "Index", "Page", new { id = Model.Id }, null) Text Session</h2>
            <p>Text Session</p>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(p => p.Id)
                @Html.HiddenFor(p => p.ShortName)
                @Html.HiddenFor(p => p.FullName)
                <div class="form-horizontal">

                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Close NOW" class="btn btn-danger" />
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>

    <div class="side-bar">
        <div class="content-block">
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

@model Warhammer.Core.Entities.Session

@{
    ViewBag.Title = "Set as Text Sessione";
}
<div class="content-wrapper">

    <div class="main-content">
        <div class="content-block">
            <h2>Setting @Html.ActionLink(Model.FullName, "Index", "Page", new { id = Model.Id }, null) as Text Session</h2>
            <p>Text Session</p>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(p => p.Id)
                @Html.HiddenFor(p => p.ShortName)
                @Html.HiddenFor(p => p.FullName)
                <div class="form-horizontal">

                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Set TEXT" class="btn btn-warning" />
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>

    <div class="side-bar">
        <div class="content-block">
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
